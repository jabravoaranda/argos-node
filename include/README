# include

Shared project headers live here.

- `ArgosNode.h`: application coordinator interface.
- `WiFiCredentials.example.h`: template for local WiFi credentials.
- `ports/`: transport-independent application ports consumed by adapters such
  as `HttpApi`.

`WiFiCredentials.h` is local-only and ignored by git.
